---
description: Start file-based planning for complex tasks.
---

Invoke the `repo-planning-conversation` skill and initialize planning files if missing.

If deterministic output is needed, run `scripts/init_planning_files.py`.
