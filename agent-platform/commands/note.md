---
description: Capture a durable mistake-prevention rule in project state.
---

Invoke the `capture-mistake-rule` skill.

If deterministic output is needed, run `agent-platform/scripts/note_rule.py`.
