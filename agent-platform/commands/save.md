---
description: Save current conversation context into .agent-state.
---

Invoke the `save-conversation` skill.

If deterministic output is needed, run `agent-platform/scripts/save_conversation.py`.

Recommended manual naming format when you want a stable human-readable conversation:

`/save --conversation <name>`
